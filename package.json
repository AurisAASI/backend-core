{
  "name": "backend-core",
  "version": "1.0.0",
  "description": "Backend core services using Serverless Framework and AWS Lambda with Python 3.10",
  "scripts": {
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:prod": "serverless deploy --stage prod",
    "remove:dev": "serverless remove --stage dev",
    "remove:prod": "serverless remove --stage prod",
    "offline": "serverless offline start",
    "test": "pytest tests --cov=src --cov-report=html --verbose",
    "format": "black src tests && isort src tests && flake8 src tests",
    "format:check": "black --check src tests && isort --check-only src tests && flake8 src tests",
    "setup:jwt:dev": "python scripts/setup_jwt_secret.py --stage dev",
    "setup:jwt:prod": "python scripts/setup_jwt_secret.py --stage prod"
  },
  "devDependencies": {
    "serverless": "^4.0.0",
    "serverless-add-api-key": "^4.2.1",
    "serverless-offline": "^14.4.0"
  }
}
